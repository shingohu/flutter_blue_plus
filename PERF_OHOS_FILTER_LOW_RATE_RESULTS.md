# OHOS 降频及监听顺序 A/B 复测

日期：2026-09-15，正式套件于北京时间 18:11–18:28 执行。本报告承接 [首轮 OHOS 真机过滤链 A/B](PERF_OHOS_FILTER_AB_RESULTS.md)，候选仍为 example 内的合并过滤子类，生产 getter 不变。

**最新落地状态：用户审阅并确认后，合并过滤已按局部 Dart 分发优化落地，详见 [生产落地记录](PERF_DART_FILTER_AB_RESULTS.md#10-生产落地记录)。** 以下正文记录落地前的实验及当时决策，回退和到达率限制继续保留。落地不表示全链路验收通过，本报告的手机测量数据没有更新或重算。

**结论：Dart 分发收益再次复现，但尚未满足生产变更的无已知回退门槛。** 十个条件的配对 P95 中位改善为 6.47%–18.04%；100 Hz 单监听器的 RTT 配对中位数在三个进程均回退，若干条件的 Dart P99 也变差。50/100 Hz 均未实际达到目标 ±5% 范围，因此本轮完成了降频及注册位置复测，仍未完成严格相同到达率的对照。候选继续留在 example。

## 1. 本轮问题与预先确定的方法

本轮只回答：降低请求事件频率、改变目标监听器位置之后，Dart 分发改善能否复现，首字段不匹配回退是否仍出现，原生通道 RTT 是否具有一致收益。分变体长时内存、Android/iOS 原生通道和 BLE/GATT 不属于本轮测量。

正式套件启动前已固定以下设计，结果不用于事后挑选或剔除配对：

- 目标频率分别为 50、100 Hz，244 B/事件；每个场景/变体 200 个事件。
- 每种频率包含 5 个条件：1 个全匹配监听器；64 个监听器、remoteId 不匹配、目标首位/末位；64 个监听器、instanceId 不匹配、目标首位/末位。
- 全局公开观察者始终先注册。目标首位/末位指 characteristic 监听器中的位置，分别为索引 0/63；单监听器只测索引 0，避免重复条件。
- 每进程每条件 4 对，10 个条件、两个变体，共 80 次场景运行；三个独立进程合计 240 次、48,000 个正式事件。每条件/变体汇总 2,400 个实际样本、12 对结果。
- 每进程等待 3 秒后，按 50/100 Hz 分别预热基线/候选，各 100 条、64 个监听器、后段不匹配、目标末位；三进程额外 1,200 条预热事件不纳入正式统计。
- 配对间交替 A/B 与 B/A，并反转条件遍历顺序；第二个进程反转初始顺序。A/B 的事件数、频率配置、注册位置、校验逻辑相同。
- 采用同一 Mate 60 Pro、OHOS Flutter SDK 和 profile 模式，静态界面、关闭插件日志，没有周期性 VM 采样。

汇总器按 **频率 × 场景 × 注册位置** 分组，不混池。汇总 P50/P95/P99 为原始样本 nearest-rank；配对改善为 `100 × (1 - fused P95 / baseline P95)` 后取 12 对的中位数。每进程仅 4 对，不以事件数冒充独立重复数，不宣称统计显著性。

预先采用两个频率核对指标：

1. 两组实际平均 Hz 相差不超过基线的 2%，记为频率接近配对。
2. 两组实际平均 Hz 均在目标 ±5% 内，记为接近设定频率配对。

这两个指标是工程诊断标记，不是统计显著性检验，也不会用于删除慢样本或只展示有利数据。实际 Hz 按原生相邻发送起点计算，同时保留间隔 P50/P95/P99。即使平均频率接近，也不能证明两条到达序列或调度环境完全一致。

## 2. 指标的解释边界

`observer_to_filtered_us` 从全局观察者记录时间，到目标 characteristic 监听器入口，使用同一 Dart Stopwatch；不包含此前的原生发送、codec 解码及 Dart 消息解析。目标首位主要观察目标自身及此前分发成本；目标末位还包含前 63 个非目标监听器的成本。二者不可混合成“单一 getter 延迟”。

`native_rtt_us` 从 ArkTS 调用生产事件通道前，到自动回复抵达 ArkTS；不是业务处理完成 ACK，不是 native-to-listener 的单向计时，也不能减去上述 Dart 指标求得纯通道耗时。

原生事件由已有 example ArkTS 定时器产生，通过真实 OHOS Engine、MethodChannel、Dart adapter 和公开 getter；没有调用真实 BLE/GATT。50/100 Hz 是请求频率，必须用实测值核对，不能直接写成设备已稳定维持的频率。

## 3. 结果

### 3.1 完整性与环境

三个独立进程的 PID 为 59326、63205、1801，分别完整导出 80 次场景运行。240 次正式场景的发送、回复、全局观察者接收、目标监听器接收均为 200 条；合计 **48,000 条正式事件，校验错误、误投递、原生错误均为 0**。每次有 200 个 Dart/RTT 样本、199 个原生间隔样本。

manifest、run token、PID、场景顺序及源码指纹检查通过。生产 getter、共享候选、生产公共 Dart 文件和原生事件发生器的 SHA256 与首轮测量一致。手机入口和运行/汇总工具按本轮矩阵调整后，在正式运行期间没有改动。

使用 Flutter `3.41.10-ohos-1.0.0` / Dart `3.11.5` 的 OHOS profile 构建，设备仍为 Mate 60 Pro `23E0224126000860`。三个签名 HAP 均成功构建、安装和运行。结果导出后 runner 退出测试应用；已恢复临时亮屏设置，并读回 ScreenOffTime=600000 ms。没有发生本轮休眠中断或超时，没有把首轮数据混入本轮。

### 3.2 Dart 分发

时间单位为 µs，箭头为基线 → 候选；“早/晚”指非目标监听器分别在 remoteId/instanceId 处不匹配。

| 目标 Hz | 条件 / 目标位置 | 汇总 P50 | 汇总 P95 | 汇总 P99 | 配对 P95 中位改善 | 改善对数 / 12 |
|---:|---|---:|---:|---:|---:|---:|
| 50 | 1 个全匹配 / 首位 | 76 → 67 | 122 → 105 | 245 → 216 | 12.86% | 12 |
| 50 | 64 早 / 首位 | 77 → 68 | 123 → 109 | 236 → 225 | 13.18% | 10 |
| 50 | 64 早 / 末位 | 249 → 232 | 430 → 376 | 695 → 553 | 13.09% | 12 |
| 50 | 64 晚 / 首位 | 76 → 68 | 130 → 108 | 258 → 275 | 14.72% | 12 |
| 50 | 64 晚 / 末位 | 2,996 → 2,490 | 3,737 → 3,054 | 4,750 → 4,039 | 18.04% | 12 |
| 100 | 1 个全匹配 / 首位 | 69 → 63 | 113 → 102 | 214 → 237 | 10.31% | 11 |
| 100 | 64 早 / 首位 | 73 → 65 | 119 → 101 | 218 → 228 | 15.97% | 12 |
| 100 | 64 早 / 末位 | 236 → 223 | 403 → 366 | 598 → 564 | 12.18% | 10 |
| 100 | 64 晚 / 首位 | 59 → 57 | 110 → 100 | 208 → 221 | 6.47% | 9 |
| 100 | 64 晚 / 末位 | 2,090 → 1,864 | 3,361 → 2,829 | 4,229 → 3,647 | 15.17% | 12 |

目标末位、后段不匹配在两个频率下均为 12/12 对 P95 改善，汇总 P95 分别降低 683/532 µs。这是本轮最一致的 Dart 收益。目标首位的汇总 P95 只降低 10–22 µs，百分比容易放大很小的绝对波动；它也没有计入后续 63 个监听器的处理成本。合并过滤仍逐监听器匹配，没有变成索引路由。

各进程内 4 对的 P95 改善中位数如下：

| 目标 Hz | 条件 / 目标位置 | 进程 1 | 进程 2 | 进程 3 |
|---:|---|---:|---:|---:|
| 50 | 1 个全匹配 / 首位 | 14.98% | 13.52% | 10.85% |
| 50 | 64 早 / 首位 | 10.37% | 13.18% | 13.46% |
| 50 | 64 早 / 末位 | 14.45% | 12.91% | 13.09% |
| 50 | 64 晚 / 首位 | 16.51% | 13.54% | 15.85% |
| 50 | 64 晚 / 末位 | 17.53% | 17.52% | 18.39% |
| 100 | 1 个全匹配 / 首位 | 8.05% | 6.86% | 18.26% |
| 100 | 64 早 / 首位 | 17.01% | 14.35% | 16.31% |
| 100 | 64 早 / 末位 | 9.64% | 10.12% | 13.26% |
| 100 | 64 晚 / 首位 | 8.08% | 0.15% | 10.63% |
| 100 | 64 晚 / 末位 | 15.11% | 13.86% | 16.78% |

首轮 200 Hz、早段不匹配、目标末位的进程中位回退没有在本轮对应的 50/100 Hz 条件中重现，但不能据此宣布首轮异常已消除。本轮仍有 8 对 Dart P95 回退：50 Hz 早/首位 2 对，100 Hz 单监听 1 对、早/末位 2 对、晚/首位 3 对。最差为 100 Hz 晚/首位的 75 → 103 µs，回退 37.33%；50 Hz 早/首位也有 126 → 161 µs，回退 27.78%。四个条件的汇总 P99 变差，见上表。100 Hz 单监听最大 Dart 样本从 557 增至 6,358 µs；这些样本均保留。

### 3.3 原生通道 RTT

时间单位为 ms；配对中位改善为负数时表示回退。

| 目标 Hz | 条件 / 目标位置 | 汇总 P50 | 汇总 P95 | 汇总 P99 | 配对 P95 中位改善 | 改善对数 / 12 |
|---:|---|---:|---:|---:|---:|---:|
| 50 | 1 个全匹配 / 首位 | 3.781 → 3.772 | 5.455 → 5.463 | 6.965 → 7.949 | 1.03% | 7 |
| 50 | 64 早 / 首位 | 4.106 → 4.037 | 5.848 → 5.881 | 8.077 → 7.829 | -1.59% | 4 |
| 50 | 64 早 / 末位 | 4.072 → 4.030 | 5.866 → 5.851 | 8.079 → 7.548 | 1.23% | 7 |
| 50 | 64 晚 / 首位 | 6.806 → 6.364 | 9.267 → 8.411 | 12.558 → 11.265 | 9.06% | 11 |
| 50 | 64 晚 / 末位 | 6.829 → 6.323 | 9.114 → 8.450 | 12.581 → 11.087 | 8.00% | 12 |
| 100 | 1 个全匹配 / 首位 | 3.452 → 3.526 | 5.140 → 5.346 | 6.431 → 7.641 | -3.10% | 4 |
| 100 | 64 早 / 首位 | 3.894 → 3.845 | 5.806 → 5.724 | 7.064 → 7.584 | -0.12% | 6 |
| 100 | 64 早 / 末位 | 3.818 → 3.816 | 5.703 → 5.783 | 6.965 → 7.277 | -1.23% | 5 |
| 100 | 64 晚 / 首位 | 5.128 → 5.361 | 8.278 → 7.917 | 14.401 → 12.647 | 3.98% | 10 |
| 100 | 64 晚 / 末位 | 5.071 → 4.826 | 8.209 → 7.832 | 12.723 → 10.581 | 9.11% | 9 |

50 Hz 晚/末位的 RTT 在 12 对均改善，三个进程的配对中位改善分别为 8.59%、9.18%、8.00%，是本轮较强的正向证据。其他后段不匹配条件也有正向进程中位结果，但须结合下一节的到达率偏差理解。

**100 Hz 单监听 RTT 的进程中位数三次均回退**：分别为 4.22%、4.31%、0.57%；合并 12 对的中位回退为 3.10%，汇总 P99 为 6.431 → 7.641 ms。该结果不符合“没有可重复延迟回退”的验收目标，不能用多监听器收益抵消。当前仍有调度/到达率等混杂因素，尚不能断言候选代码是唯一原因，也不能未验证就归因于 Engine 并忽略。

此外，100 Hz 早/末位的最大 RTT 为 34.090 → 72.460 ms；所有长尾保留，没有删异常后重算收益。

### 3.4 实际频率

| 目标 Hz | 条件 / 目标位置 | 基线实际 Hz 范围 | 候选实际 Hz 范围 | 两组 Hz 差 ≤2% 的配对 / 12 |
|---:|---|---:|---:|---:|
| 50 | 1 个全匹配 / 首位 | 45.45–46.73 | 45.86–46.66 | 12 |
| 50 | 64 早 / 首位 | 45.52–46.06 | 45.29–46.14 | 12 |
| 50 | 64 早 / 末位 | 45.02–46.37 | 45.18–46.56 | 12 |
| 50 | 64 晚 / 首位 | 40.91–44.37 | 42.03–43.41 | 6 |
| 50 | 64 晚 / 末位 | 41.35–42.87 | 42.03–43.78 | 7 |
| 100 | 1 个全匹配 / 首位 | 85.21–89.31 | 83.35–88.42 | 7 |
| 100 | 64 早 / 首位 | 83.45–85.51 | 82.80–86.20 | 9 |
| 100 | 64 早 / 末位 | 83.65–86.55 | 82.48–86.64 | 10 |
| 100 | 64 晚 / 首位 | 72.77–80.16 | 72.18–78.79 | 5 |
| 100 | 64 晚 / 末位 | 74.37–79.51 | 73.55–88.67 | 4 |

120 对中 84 对满足组间平均 Hz 差 ≤2%；**0 对满足两组均接近目标 ±5%**。50 Hz 请求实际约 40.91–46.73 Hz，100 Hz 请求实际约 72.18–89.31 Hz。降低请求频率没有自动获得精确的固定到达率。

汇总原生间隔也有波动，例如 50 Hz 单监听基线间隔 P50/P95/P99 为 21.649/23.419/24.270 ms；100 Hz 晚/末位基线为 13.198/15.915/17.177 ms，候选为 12.820/15.489/16.760 ms。更详细的每组及每对间隔统计保留在 summary.json。

本轮仍是相同定时器配置、相同事件数下的真实运行对照，不是严格同到达率实验，也不是 BLE 吞吐测试。报告中的全部收益使用全部配对，未只选出这 84 对重新宣称收益。绝对耗时不与首轮 200 Hz 直接比较：本轮频率、注册位置、每场景事件数和运行时状态均不完全相同。未采集 CPU 频率或温度，不能把绝对耗时变化解释为已证实的降频/温控原因。

### 3.5 内存与三端边界

混合 A/B 进程的 PSS 端点如下，终点在全部事件结束、冷却 10 秒后采集：

| PID | 起点 PSS（KB） | 终点 PSS（KB） | 增量（KB） |
|---:|---:|---:|---:|
| 59326 | 77,510 | 166,919 | 89,409 |
| 63205 | 83,690 | 168,521 | 84,831 |
| 1801 | 83,650 | 167,267 | 83,617 |

进程中包含基线、候选、预热、运行时及保留的完整样本，无法据此进行分变体归因，也不能证明无内存回退或将全部增长归为计时数组/Engine。用户已允许不修复 Engine，本轮没有展开 Engine 修复；候选自身的独立内存对照仍需另做。

Android、iOS/Darwin 继续保留此前 33 项三端 Dart 通道行为测试的覆盖，本轮候选源码未变，未重复运行这 33 项测试。本轮只增加了 OHOS 的真实通道证据，没有新增 Android/iOS 原生构建或手机性能数据，也没有验证 GATT、无线、断连和服务重发现。

### 3.6 决策与后续优先级

本轮加强了“减少多层过滤可降低 Dart 分发成本”的证据，特别是目标位于末位的多监听器场景；同时明确暴露了单监听 RTT 的重复回退和部分 P99 回退。**当前不将候选合入生产 getter，不扩展到其它响应流，也不同时引入二进制通道、事件索引或原生缓存。**

后续优先安排基线/候选独立进程、低采样保留的内存对照；若继续复核 RTT，先测空通道/事件发生器的实际节拍和测量开销，再设计可比较的到达序列。该工作是验证测量方法与候选，不要求修复 OHOS Engine。Android/iOS 仍需各自的原生模拟入口及实际运行结果，有可用环境才测，不能套用本报告的百分比。

## 4. 工具验证与复现

手机入口及共享候选的 Flutter 静态分析通过；汇总器 6 项测试通过，覆盖旧格式兼容、频率/注册位置分组、错误工作量及顺序拒绝、陈旧 token/重复 PID 拒绝、异常计数/样本拒绝，以及保留频率不一致的回退配对。合成夹具只测试计算逻辑，不计作手机测量。

保持设备解锁亮屏。在 `packages/flutter_blue_plus/example` 运行，输出目录必须不存在：

```sh
node tool/run_filter_ab.mjs \
  /Users/shingo/develop/SDK/ohos_flutter/bin/flutter \
  /Applications/DevEco-Studio.app/Contents/sdk/default/openharmony/toolchains/hdc \
  23E0224126000860 perf_results/NEW_LOW_RATE_DIRECTORY low-rate
node tool/summarize_filter_ab.mjs perf_results/NEW_LOW_RATE_DIRECTORY
node --test tool/summarize_filter_ab.test.mjs
```

省略末尾 `low-rate` 时仍使用原 200 Hz 工作量；旧报告对应的数据不得与本轮合并。每次运行有独立 PID、run token 和源码 SHA256，runner 拒绝中途修改被测源码或复用旧输出目录。本轮正式目录为 `packages/flutter_blue_plus/example/perf_results/2026-09-15-ohos-filter-low-rate`。

构建沿用用户已授权的 OHOS Flutter SDK，跳过缺失 CodeGenie 前置检查，未修改签名或原生实现。若临时设置 PowerManager 保持亮屏，须在成功或失败后恢复；runner 本身不管理此设置。

原始结果与工具：

- [正式 manifest 与源码指纹](packages/flutter_blue_plus/example/perf_results/2026-09-15-ohos-filter-low-rate/manifest.json)。
- [汇总、全部 120 对结果及频率标记](packages/flutter_blue_plus/example/perf_results/2026-09-15-ohos-filter-low-rate/summary.json)。
- [进程 1 原始样本](packages/flutter_blue_plus/example/perf_results/2026-09-15-ohos-filter-low-rate/run1.json)、[进程 2](packages/flutter_blue_plus/example/perf_results/2026-09-15-ohos-filter-low-rate/run2.json)、[进程 3](packages/flutter_blue_plus/example/perf_results/2026-09-15-ohos-filter-low-rate/run3.json)；同目录保留构建及运行日志。
- [手机入口](packages/flutter_blue_plus/example/lib/filter_ab_main.dart)、[矩阵校验配置](packages/flutter_blue_plus/example/tool/filter_ab_plan.mjs)、[汇总器](packages/flutter_blue_plus/example/tool/summarize_filter_ab.mjs)、[汇总器测试](packages/flutter_blue_plus/example/tool/summarize_filter_ab.test.mjs)。

只核对本次数据时，将汇总器参数设为 `perf_results/2026-09-15-ohos-filter-low-rate` 即可，无需重新构建或运行设备。
